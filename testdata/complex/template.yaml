encoded:
  $eval: encode(["n-za-mN-ZA-M"], "foo")
decoded:
  $eval: decode(["a-zA-Z"], encode(["b-zaB-ZA"], "foooooo"))
raw: ${rawparam}
a: ${foo}
b: ${bar}
c: ${foobar}
d: ${text}
stuff:
  $eval: list
fileinfo:
  $eval: withfilename
